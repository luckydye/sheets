diff --git a/webapp/IronCalc/src/components/WorksheetCanvas/util.ts b/webapp/IronCalc/src/components/WorksheetCanvas/util.ts
index 56a5a8d..8adad72 100644
--- a/webapp/IronCalc/src/components/WorksheetCanvas/util.ts
+++ b/webapp/IronCalc/src/components/WorksheetCanvas/util.ts
@@ -15,6 +15,12 @@ export function hexToRGBA10Percent(colorHex: string): string {
   return `rgba(${red}, ${green}, ${blue}, ${alpha})`;
 }
 
+/**
+ * No column is wide enough to fit this many characters, so this is how much of a
+ * line has to be measured to know that the whole of it does not fit.
+ */
+const PROBE_LENGTH = 512;
+
 /**
  * Splits the given text into multiple lines. If `wrapText` is true, it applies word-wrapping
  * based on the specified canvas context, maximum width, and horizontal padding.
@@ -28,6 +34,9 @@ export function hexToRGBA10Percent(colorHex: string): string {
  * @param wrapText Whether to apply word-wrapping or just return text split by newlines.
  * @param context  The `CanvasRenderingContext2D` used for measuring text width.
  * @param width    The maximum width for each line.
+ * @param maxLines Stop after this many lines. Wrapping measures every word, so a
+ *                 cell holding thousands of characters is expensive; the caller
+ *                 passes a limit when it knows the rest would be clipped away.
  * @returns        An array of lines (strings), each fitting within the specified width if wrapping is enabled.
  */
 export function computeWrappedLines(
@@ -35,6 +44,7 @@ export function computeWrappedLines(
   wrapText: boolean,
   context: CanvasRenderingContext2D,
   width: number,
+  maxLines = Number.POSITIVE_INFINITY,
 ): string[] {
   // Split the text into lines
   const rawLines = text.split("\n");
@@ -44,8 +54,26 @@ export function computeWrappedLines(
   }
   const wrappedLines = [];
   for (const line of rawLines) {
+    if (wrappedLines.length >= maxLines) {
+      return wrappedLines;
+    }
+    // A line that fits as it is needs no wrapping. Worth a look before measuring
+    // it word by word, which costs a measurement per word.
+    // Only the start of the line is measured: measuring a very long string is
+    // slow in itself, and if its first characters are already too wide for the
+    // cell then so is the whole thing.
+    const probe =
+      line.length > PROBE_LENGTH ? line.slice(0, PROBE_LENGTH) : line;
+    if (
+      context.measureText(probe).width < width &&
+      probe.length === line.length
+    ) {
+      wrappedLines.push(line);
+      continue;
+    }
     const words = line.split(" ");
     let currentLine = words[0];
+    let truncated = false;
     for (let i = 1; i < words.length; i += 1) {
       const word = words[i];
       const testLine = `${currentLine} ${word}`;
@@ -54,10 +82,16 @@ export function computeWrappedLines(
         currentLine = testLine;
       } else {
         wrappedLines.push(currentLine);
+        if (wrappedLines.length >= maxLines) {
+          truncated = true;
+          break;
+        }
         currentLine = word;
       }
     }
-    wrappedLines.push(currentLine);
+    if (!truncated) {
+      wrappedLines.push(currentLine);
+    }
   }
   return wrappedLines;
 }
diff --git a/webapp/IronCalc/src/components/WorksheetCanvas/worksheetCanvas.ts b/webapp/IronCalc/src/components/WorksheetCanvas/worksheetCanvas.ts
index 4473eb0..978cce7 100644
--- a/webapp/IronCalc/src/components/WorksheetCanvas/worksheetCanvas.ts
+++ b/webapp/IronCalc/src/components/WorksheetCanvas/worksheetCanvas.ts
@@ -130,6 +130,29 @@ export default class WorksheetCanvas {
    */
   scrollOffset: ScrollOffset;
 
+  /**
+   * The lines a cell's text breaks into, and how wide each one is. See
+   * getCellLines: wrapping is the most expensive thing a frame does, and
+   * scrolling redraws the same cells without their text changing.
+   */
+  private cellLines: Map<
+    number,
+    {
+      font: string;
+      width: number;
+      budget: number;
+      lines: string[];
+      widths: number[];
+    }
+  >;
+
+  /**
+   * Set by setScrollPosition: the next render only moves the view, so the wrapped
+   * text of each cell is still good. Any other render starts from scratch, since
+   * it may be following an edit.
+   */
+  private preserveCellLines: boolean;
+
   constructor(options: CanvasSettings) {
     this.model = options.model;
     this.sheetWidth = 0;
@@ -158,6 +181,8 @@ export default class WorksheetCanvas {
     // a cell marked as "spill" means its left border should be skipped
     this.spills = new Map<string, number>();
     this.cells = [];
+    this.cellLines = new Map();
+    this.preserveCellLines = false;
   }
 
   setScrollPosition(scrollPosition: { left: number; top: number }): void {
@@ -168,6 +193,7 @@ export default class WorksheetCanvas {
     //    3. (left, top) are the absolute coordinates of a cell
     // Whatever is left over is kept as a sub-cell pixel offset, so that we can
     // render partially scrolled rows/columns and scroll smoothly.
+    this.preserveCellLines = true;
     const { column, left } = this.getBoundedColumn(scrollPosition.left);
     const { row, top } = this.getBoundedRow(scrollPosition.top);
     this.scrollOffset.x = Math.max(0, scrollPosition.left - left);
@@ -569,6 +595,53 @@ export default class WorksheetCanvas {
     }
   }
 
+  /**
+   * The lines a cell's text breaks into, with the width of each.
+   *
+   * Wrapping measures every word of the text, so for a sheet with a lot of text
+   * in its cells this is by far the most expensive part of drawing a frame — and
+   * scrolling draws the same cells over and over, with the same text. Hence the
+   * cache; see `preserveCellLines` for when it is dropped.
+   */
+  private getCellLines(
+    sheet: number,
+    row: number,
+    column: number,
+    font: string,
+    wrapText: boolean,
+    width: number,
+    lineBudget: number,
+  ): { lines: string[]; widths: number[] } {
+    const key = row * (LAST_COLUMN + 1) + column;
+    const cached = this.cellLines.get(key);
+    if (
+      cached &&
+      cached.font === font &&
+      cached.width === width &&
+      cached.budget === lineBudget
+    ) {
+      return cached;
+    }
+    const context = this.ctx;
+    context.font = font;
+    const text = this.model.getFormattedCellValue(sheet, row, column);
+    const lines = computeWrappedLines(
+      text,
+      wrapText,
+      context,
+      width - cellPadding,
+      lineBudget,
+    );
+    const widths = lines.map((line) => context.measureText(line).width);
+    // A long scroll through a big sheet would otherwise grow this without end
+    if (this.cellLines.size > 4096) {
+      this.cellLines.clear();
+    }
+    const entry = { font, width, budget: lineBudget, lines, widths };
+    this.cellLines.set(key, entry);
+    return entry;
+  }
+
   // Compute the text properties for a cell
   private computeCellText(
     row: number,
@@ -599,19 +672,24 @@ export default class WorksheetCanvas {
     const context = this.ctx;
     context.font = font;
     context.fillStyle = textColor;
-    const fullText = this.model.getFormattedCellValue(
-      selectedSheet,
-      row,
-      column,
-    );
 
     // Is there any better to determine the line height?
     const lineHeight = fontSize * 1.5;
-    const lines = computeWrappedLines(
-      fullText,
+    // Text hanging from the top of the cell is clipped at its bottom edge, so
+    // wrapping past that is invisible work — and a cell can hold a _lot_ of text.
+    // The other alignments show the last lines instead, so they need all of them.
+    const lineBudget =
+      wrapText && verticalAlign === "top"
+        ? Math.ceil(height / lineHeight) + 1
+        : Number.POSITIVE_INFINITY;
+    const { lines, widths } = this.getCellLines(
+      selectedSheet,
+      row,
+      column,
+      font,
       wrapText,
-      context,
-      width - cellPadding,
+      width,
+      lineBudget,
     );
     const lineCount = lines.length;
     let maxWidth = 0;
@@ -633,7 +711,7 @@ export default class WorksheetCanvas {
     };
 
     lines.forEach((text, line) => {
-      const textWidth = context.measureText(text).width;
+      const textWidth = widths[line];
       let textX: number;
       let textY: number;
       // The idea is that in the present font-size and default row heigh,
@@ -1915,6 +1993,10 @@ export default class WorksheetCanvas {
   }
 
   renderSheet(): void {
+    if (!this.preserveCellLines) {
+      this.cellLines.clear();
+    }
+    this.preserveCellLines = false;
     const context = this.ctx;
     const { canvas } = this;
     const selectedSheet = this.model.getSelectedSheet();
